#Information about A-interaction matrix and X-time series data

S=60 #matrix size
mu=-1 #mean
sigma=0.5 #deviation
rhoB=-0.5 #correlation
d=10 #diagonal elements
N=100 #number of data points per species


#Eg=eigen(A)
# minEg=min(Re(Eg$values))
# maxEg=max(Re(Eg$values))